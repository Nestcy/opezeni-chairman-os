Remove the selected static console screen markup from `src/components/site/CinematicOpening.tsx` (around line 217). This eliminates the fake notification/dashboard text that appears inside the phone during the zoom transition, leaving the cinematic sequence clean.